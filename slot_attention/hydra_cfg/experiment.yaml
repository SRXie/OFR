# A yaml version of params.py

seed: 3
gpu_id: '0'
lr: 0.0004
batch_size: 64
val_batch_size: 64
resolution: [64, 64] # Tuple[int, int]
num_slots: 7
num_iterations: 4
data_root: /datasets01/CLEVR_v1.0/060817/
test_root: /checkpoint/siruixie/clevr_obj_test/output/
gpus: 1
max_epochs: 100
num_sanity_val_steps: 1
scheduler_gamma: 0.5
weight_decay: 0.0
num_train_images: null # Optional[int]
num_val_images: null  # Optional[int]
num_test_images: 1
empty_cache: True
is_logger_enabled: True
is_verbose: True
num_workers: 4
n_samples: 5
warmup_steps_pct: 0.02
decay_steps_pct: 0.2
test_type: obj # or "attr"

hydra:
  run: 
    dir: outputs/${hydra.job.override_dirname}

